---
date: 2026-07-25
scope: 全件（両台帳・初回横断集約）
---

# 要因分析 横断集約 — 2026-07-25

> **本レポートの位置づけ**: issue #21（クローズ済み 13 本の初回バックフィル）に伴う**横断集約の初実走**。
> 規範は [`.kiro/steering/moira-change-analysis.md`](../../steering/moira-change-analysis.md) §1 A5・§6。
> **本レポートは確定文書を一切書き換えない**（同 §11）。採択された Try は本 issue では適用せず、
> 別 issue として起票して `moira-change` に流す（issue #21 の HY 規定）。

## 対象

| | 件数 |
|---|---|
| 障害台帳（`.kiro/postmortem/defects.md`） | 8（うち `Schema: v1` **1 件** = #0001） |
| 変更分析台帳（`.kiro/analysis/entries/`） | 7（全件 `schema: v2`） |
| **合計** | **15 entry** |

**集計から外した entry**: なし。#0001 は `Schema: v1`（旧 10 項目）だが、規範 §10・D-86 に従い
**malformed にせず**、欠落する 6 項目相当を `unknown` として集計に載せた。

**変更キーとの対応**: `.kiro/analysis/INDEX.md` は **14 行**（`moira#1/2/5/6/7/8/9/10/11/13/15/16/17/19`）。
#0001 は変更に紐づかない不具合（`Key: 該当なし`）のため INDEX 行を持たない——**entry 15 ≠ INDEX 14**
の差はこの 1 件に由来する。母集団は `gh issue list --repo PrimeBrains/moira --state all`（2026-07-25 時点）の
CLOSED 14 本と一致し、**未分析キューは 0**。

## 4 軸頻度

集計母数は**変更キーを持つ 14 entry**（#0001 は変更分類・対象システムが `unknown` のため 4 軸頻度からは除外し、
出所の健全性の節でのみ扱う）。

### 変更分類（R15）

| ラベル | 件数 | 割合 | 内訳（障害／非障害） |
|---|---|---|---|
| `bugfix` | 6 | 43% | 障害 6 ／ 非障害 0 |
| `process-improve` | 3 | 21% | 障害 0 ／ 非障害 3 |
| `req-add` | 3 | 21% | 障害 1（`moira#11`）／ 非障害 2 |
| `req-change` | 1 | 7% | 障害 0 ／ 非障害 1 |
| `doc-only` | 1 | 7% | 障害 0 ／ 非障害 1 |

**障害 7・非障害 7**（`moira#9` を含む）。

> **本軸の運用上の注意（機構課題 G1）**: R15 は媒体軸（`doc-only`）と目的軸（`bugfix`）を混在させている。
> 本バックフィルでは「**障害と判定した件は目的側（`bugfix`）を採る**」という規則を全件に一律適用した
> （`moira#5`・`moira#8` が該当）。この規則は正本タクソノミーに書かれていない——後述の G1 として申し送る。

### 根本要因分類（R14）

| ラベル | 件数 | 割合 | キー |
|---|---|---|---|
| `verification-gap` | 4 | 29% | `moira#1` `moira#8` `moira#15` `moira#19` |
| `context-loss` | 3 | 21% | `moira#5` `moira#16` `moira#17` |
| `assumption-error` | 2 | 14% | `moira#2` `moira#9` |
| `spec-impl-mismatch` | 2 | 14% | `moira#7` `moira#11` |
| `state-management-gap` | 2 | 14% | `moira#10` `moira#13` |
| `other` | 1 | 7% | `moira#6` |

（#0001 も `verification-gap` を持つ——本軸に限れば **5 件**で最大クラスタ。）

### 対象システム（R13）

親ラベルで集計。複数ラベルを持つ entry は各ラベルに計上（延べ 28 ラベル / 14 entry）。

| ラベル | 件数 | 割合（14 entry 中） |
|---|---|---|
| `process` | 13 | 93% |
| `backend` | 6 | 43% |
| `cli` | 6 | 43% |
| `frontend` | 2 | 14% |
| `adapter` | 1 | 7% |
| `other` | 0 | 0% |

**`process` が 14 分の 13**。Moira の変更管理は現在ほぼプロセス側（正典・skill・steering・台帳）を
動かしており、参照実装への変更はその追随として現れる——というのが母集団の実像である。

### 検知工程ギャップ（検知すべき → 実際に検知した）

| ペア | 件数 | キー | すり抜けた層の解釈 |
|---|---|---|---|
| `p2-impact-survey` → `post-close` | 1 | `moira#5` | 移管変更の影響調査が「本文中の環境前提」を波及先に数えなかった |
| `ci` → `gate-adversary` | 1 | `moira#8` | リンク解決の機械検査が不在で、検出が目視層まで下流化した |
| `ha-ratification` → `post-close` | 1 | `moira#9` | 既定の前提（誰の運用か）を批准時に問えていなかった |
| `gate-adversary` → `p5-closure` | 1 | `moira#10` | 敵対レビューが「並行セッション下でも成り立つか」を攻撃角に持たなかった |
| `ha-ratification` → `p2-impact-survey` | 1 | `moira#11` | 批准した意図の正典文面への着地を追跡する配線が無かった |
| `ci` → `user-report` | 1 | `moira#13` | 故障条件（env 設定済みシェル）で走る検証層が存在しなかった |
| `p5-closure` → `gate-adversary` | 1 | `moira#15` | 閉包が「正典の列挙 n 件に対する被覆 n 件」を数えなかった |
| `p2-impact-survey` → `gate-adversary` | 1 | `moira#17` | 列挙をファイル単位で行ったため経路単位の漏れ（`import members`）を許した |
| `gate-adversary` → `gate-judge` | 1 | `moira#19` | 著者側に「直した」の現物確認義務が無く、独立採点が 2 度 FAIL するまで残骸が見えなかった |
| `unknown` → `post-close` | 1 | `moira#1` | 検知すべき工程を特定する根拠が証跡に無い（後述の G5） |
| **`gate-adversary` → `gate-adversary`**（ギャップなし） | 1 | `moira#2` | 工程が設計どおり働いた |
| **`p5-closure` → `p5-closure`**（ギャップなし） | 2 | `moira#6` `moira#7` | 同上（`moira#2` の閉包が deferred として正しく切り出した） |
| **`p2-impact-survey` → `p2-impact-survey`**（見かけ上ギャップなし） | 1 | `moira#16` | **偽のギャップなし**——検知したのは #15 の P2、検知すべきだったのは #11 の P2。R4 が「どの issue のその工程か」を表せない（G2） |

**ギャップあり 10 件／ギャップなし 4 件**（うち 1 件は偽の「なし」）。
実質は **11 件がすり抜け**、真に設計どおり働いたのは 3 件（`moira#2`・`moira#6`・`moira#7`）である。

**すり抜け先の分布**: `post-close`／`user-report` へ落ちたもの 4 件（`moira#1` `moira#5` `moira#9` `moira#13`）——
すなわち**全内製ゲートを抜けた件が 4 件ある**。残る 6 件は下流ゲート（`gate-adversary` 3・`p5-closure` 1・
`gate-judge` 1・`p2-impact-survey` 1）が受け止めた。

## クラスタ

### A. 追跡の欠落（批准・deferred・維持義務が成果物へ着地したかを誰も数えない）

- **キー**: `moira#1`（維持義務欄の更新契機が未設計）・`moira#2`（§7 残余に再評価契機が無い）・
  `moira#9`（既定の前提が批准時に問われない）・`moira#11`（批准意図と正典文面の乖離＝`[intent-drift]`）
- **共通形**: 何かを「決めた／残した」記録は残るが、**その決定が成果物のどこに着地したか**を追う行が無い。
- **代表 Try**: T1・T6

### B. 同型掃射の不在（1 箇所直したとき、同じ形が他に何箇所あるかを数えない）

- **キー**: `moira#5`（文書の旧リポ前提）・`moira#15`（正典 4 例中 3 例未被覆）・
  `moira#16`（events.json）・`moira#17`（残余 6 ファイル＋7 経路目）・`moira#8`（リンク 23 本）
- **共通形**: 課題文が名指しした対象に視野が固定され、同型の全域走査が行われない。
  **`moira#15` → `#16` → `#17` は 3 回に分けて同じ形の欠陥を追いかけた連鎖**であり、本クラスタ最大の証拠。
  `moira#17` はさらに強い——**6 ファイルを明示列挙してなお 7 つ目の経路が漏れた**。
- **代表 Try**: T2（手続き側）・T3／T4（機械側）
- **重要な含意**: `moira#17` は「**工程を足すだけでは防げない**ことの直接証拠」である。
  列挙・裁定・実装の 3 工程すべてが同一の盲点を共有した以上、独立した機械の目が要る。

### C. 自己申告の未検証（「直した」「守っている」を現物で確かめる層が無い）

- **キー**: `moira#19`（著者の是正申告を検証する層が往復に無い）・`moira#8`（リンク健全性を機械が見ない）・
  `moira#13`（テスト隔離を機械が見ない）・`#0001`（サブエージェント成功報告を検証する層が無い）
- **共通形**: 検証手段そのものが未整備で、著者／実行者の申告がそのまま通過する。
- **代表 Try**: T4・T5・T7

### 対象システム別クラスタ

- `process`（13 件）: 上記 A/B/C のすべてがここに集中する。
- `backend` ＋ `cli`（6 件ずつ・重複大）: 永続化層に集中（`moira#15/16/17`）＋訂正層（`moira#6/11`）。
- `frontend`（2 件）・`adapter`（1 件）: 単発。クラスタを形成しない。

### 同件 cross-reference（参照グラフ）

```
moira#2 ──deferred──> moira#6 ──deferred──> moira#11 ──残余開示──> moira#15
   └────deferred────> moira#7 ──ゲート指摘──> moira#8                  │
                                                                      ├──> moira#16 ──> moira#17
moira#5 ─(同型: context-loss)─ moira#16 / moira#17 ─(同型)─ moira#20 [OPEN]
moira#13 ─(実害として顕在化)─ moira#16 の欠陥
moira#10 ─(同型: state-management-gap)─ moira#13
#0001 ─(同型: verification-gap)─ moira#8 / moira#15 / moira#19
```

**注目**: `moira#2` を起点に **6 本の連鎖**が伸びている（#6 → #11 → #15 → #16 → #17 と #7 → #8）。
1 つの正典改訂が、追随の追随として 6 本の後続 issue を生んだ。これは欠陥ではなく設計どおりの
deferred 追跡の働きだが、**連鎖の各段で同型掃射が漏れた**（クラスタ B）。

**未解消の同型**: `moira#20`（OPEN・todo-playground に旧版 postmortem skill 一式が残存）は
`moira#5` / `moira#16` / `moira#17` と同一クラスタの**現存例**である。

## 出所の健全性（本機構の正直枠の自己点検）

`Schema: v2` の 14 entry（14 × 16 = **224 欄**）の内訳:

| ラベル | 欄数 | 割合 |
|---|---|---|
| `derived` | 85 | 37.9% |
| `inferred` | 135 | **60.3%** |
| `captured` | 2 | 0.9% |
| `unknown` | 2 | 0.9% |

`Schema: v1` の #0001 を規範 §10 の写像で 16 欄に展開すると `unknown` が 8 欄増える
（対象システム・変更分類・変更範囲・発生原因サマリ／詳細・同件調査対象・同件の対応状況・検知対策）。
**全 15 entry・240 欄**では `derived` 35.4% ／ `inferred` 56.3% ／ `captured` 0.8% ／ `unknown` 4.2% ／
出所ラベル未記録（v1 様式・8 欄）3.3%。

**`unknown` が集中している項目**: **項目 13（検知すべき工程）1 件・項目 15（検知できなかった理由）1 件**
——いずれも `moira#1`。集中と呼べるほどの偏りは無い（v2 全 224 欄中 2 欄）。

### issue #21 の予測との差（正直開示）

issue #21 本文は「**13 本すべて `captured` は 0 になる**」と予測していたが、**外れた**。
`moira#19` は項目 3（障害判定）・項目 4（変更分類）が `captured` である——一次採取の 2 欄を導入したのが
issue #19 自身であり、その P6 閉包レポートに採取値が残っていたため。**自己適用が予測を破った**形になる。
一次採取の導入（2026-07-25）以降にクローズした変更では `captured` が 2 欄ずつ入るため、
次回以降の集約ではこの割合が上がるはずである——**それが上がらなければ P6 の採取が働いていない兆候**になる。

### `inferred` 60% をどう読むか

過去分の後知恵再構成であるため `inferred` 支配は想定内であり、**それ自体は仕組みの穴ではない**。
ただし本バックフィルの結論——とりわけクラスタ A/B/C と Try 群——は、その 60% の上に立っている。
`derived` 37.9% は台帳（`moira/changes/issue-N/`）と GitHub コメントが十分に厚かったことの反映であり、
台帳が無ければこの分析は成立しなかった。

## Try 候補

**裁定は 2026-07-25 の HY で全件 approved**（ユーザー「全て承認します」）。
**ただし本 issue（#21）では一切適用しない**——issue #21 の DoD が「採択された Try は別 issue を立てて
`moira-change` に流す」と規定しているため。下表の「出口の名指し」は起票時の変更要求文の材料である。

| # | Try | 根拠 entry | 出口の名指し | 裁定 |
|---|---|---|---|---|
| T1 | **HA 裁定を「行」にする**——各裁定に (a) 着地先の面と成果物パス (b) 反映済みか否かの二択を持たせ、P5 閉包で全行の反映を照合する（#11 以降の閉包レポートが持つ対応表を規範へ昇格） | `moira#11` `moira#9` | `.kiro/steering/moira-change-management.md` §3/§5 ＋ `moira/changes/templates/closure-report.template.md` | **approved** |
| T2 | **同型掃射の P2 必須化**——(a) 同型パターンを grep 式として書き (b) 全ヒットを影響マップの行へ写像し (c) 閉包で残存 0 を機械証跡で示す。正典が列挙した n 件は n 行へ展開する。影響マップに「配布経路に乗るか」列を追加 | `moira#5` `moira#15` `moira#16` `moira#17` | `.claude/skills/moira-change/SKILL.ja.md` P2 ＋ `moira/changes/templates/impact-map.template.md` | **approved** |
| T3 | **`fs.writeFileSync` 直呼びの静的禁止**——`moira/backend/src/atomic-write.ts` 以外での直呼びを CI で fail（既知の例外＝`init` の create-once シードは許可リストで明示） | `moira#16` `moira#17` | `.dependency-cruiser.cjs` または ESLint ルール ＋ CI | **approved** |
| T4 | **md 相対リンク解決の CI 検査**——`.kiro/scenarios/units/`・`moira/`・`.kiro/steering/`・`.claude/skills/` の相対リンクを解決し未解決を fail（意図的な来歴リンクは許可リスト） | `moira#8` | CI（新規 job） | **approved** |
| T5 | **`doc-refine` に著者側の残存 0 証跡義務**——「直した」と申告する際、指摘語の全語形を grep して残存 0 を同一ラウンド内で示す | `moira#19` | `.claude/skills/doc-refine/SKILL.ja.md` | **approved** |
| T6 | **維持義務欄の更新契機の明記**——確定文書の「維持義務のある欄」に更新契機と検出手段を書く。MODEL §7「既知の残余」の各行に owner と再評価条件を持たせる | `moira#1` `moira#2` | `.claude/skills/doc-refine/` 確定チェック ＋ `.kiro/steering/moira-model.md` | **approved** |
| T7 | **テスト隔離の明文化＋陰性対照の CI 常設**——(a) 「テストは実行環境の env を継承しない」を規約化 (b) `MOIRA_DIR=<canary>` 下でスイートを走らせ canary 配下に `.moira/` が生成されたら fail するジョブを常設 | `moira#13` | `.kiro/steering/testing-conventions.md` ＋ CI | **approved** |

### Try の性質による仕分け（機械 vs 手続き）

- **機械（計器）で塞ぐ**: T3・T4・T7(b) — いずれも判定が完全に機械化でき、人間の記憶に依存しない。
- **手続き（規範・テンプレート）で塞ぐ**: T1・T2・T5・T6・T7(a)。
- **`moira#17` の教訓**: 手続き側だけでは列挙の漏れを塞げない。**T2 は必ず T3 と対で入れる**こと。
- **新しいゲートは 1 つも提案していない**——既存のゲート・工程・計器に義務を足す形に収めた。
  ゲートを増やすことは工程を重くし、`moira#17` が示すとおり漏れの根治にはならないため。

## 本機構（要因分析フロー）自身の課題

初回実走で見つかった**機構側の欠陥**。issue #21 の DoD「機構の欠陥は別途起票」に従い、
**Try とは別の issue として起票する**（本レポートでは記録のみ）。

| # | 課題 | 根拠 | 影響 |
|---|---|---|---|
| G1 | **R15 変更分類が媒体軸と目的軸を混在**（`doc-only` は媒体・`bugfix` は目的）。同一の変更がどちらにも当たる | `moira#5` `moira#8` | 本バックフィルでは「障害判定件は目的側」という**正本に無い規則**を作って回避した。規則を正本へ昇格するか、軸を分けるかの裁定が要る |
| G2 | **R4 検知工程が「どの issue のその工程か」を表せない** | `moira#16` `moira#17` | 1〜2 issue ぶん遅れた検知が、ラベル上「ギャップなし」に見える。**すり抜けを過小評価する方向の誤り**であり実害がある |
| G3 | **1 entry 1 判定・1 ラベルの様式が、混在する変更を表現できない** | `moira#11`（障害成分と計画的追加が混在）`moira#19`（是正対象の欠陥 8 件に 1 ラベル） | 判定・分類が実態より粗くなる |
| G4 | **R3／R14 に「分類対象たる失敗が無い」値が無い** | `moira#6` | 失敗が無い変更が `other` に倒れ、`other` が「未分類」と「該当なし」の 2 義になる |
| G5 | **検知系 4 項目（13〜16）が非障害に適用しづらい** | `moira#9` 項目 15・`moira#1` 項目 13/15 | 非障害では `unknown` が構造的に出る。**本機構の `unknown` 2 欄はどちらもここに由来する** |
| G6 | **すり抜け検出ログが空のまま** | `.kiro/analysis/INDEX.md` 下部の表 | 当該表は「検出したその場で 1 行残す」設計であり、バックフィルでは要件を満たせない。**初回実走では 11 件のすり抜けがあったのに 0 行**という状態が残る——運用開始点の宣言が要る |

## 反映結果

| Try | 反映先 | 結果 |
|---|---|---|
| T1〜T7 | 別 issue（`moira-change` 経由） | **本 issue では未着手**（issue #21 の HY 規定により意図的に適用しない） |
| G1〜G6 | 別 issue（機構の是正） | **本 issue では未着手**（同 DoD「機構の欠陥は別途起票」） |

**`.kiro/steering/` への書き込みは 0 件**。`.kiro/postmortem/defects.md` の
`## Steering 反映ログ` も**未追記**（反映が発生していないため）。

## entry の状態遷移

本集約の完了により:

- 非障害 7 件（`entries/moira-{1,2,6,7,9,10,19}.md`）: `ratified` → **`aggregated`**
- 障害 8 件（`defects.md` #0001〜#0008）: `recorded` → **`reviewed`**

`steered` への遷移は、上記 Try が別 issue で実際に steering へ反映されたときに行う。
