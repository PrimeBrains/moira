---
status: working-ledger
issue: 19
---

# ゲート往復記録 — issue #19（要因分析機構）

`doc-refine`（D 級＋F 級を 1 ゲートに束ねた）の往復記録。**指摘 1 件ごとの disposition**（修正／健全な反証／
追跡付き deferred）を残す——ゲート基準「反論されない Critical = 0 ＋ Important 全件 disposition」の証跡。

> 本環境では **codex 非在**のため、外部ベンダーレビューの代替として独立 subagent（`doc-adversary`・
> `doc-fact-checker`・`doc-gate-judge`）を用いた（#16 の先例と同型・正直開示）。

## ラウンド 1

### 参加者と結果

| 役 | 主体 | 結果 |
|---|---|---|
| 敵対レビュー | `doc-adversary`（独立） | Critical 7・Important 13・Minor 4 |
| 事実検証 | `doc-fact-checker`（独立） | `OBJECTIONS_REMAIN`（CORRECTED 6 件・CONFIRMED 多数） |
| 独立採点 | `doc-gate-judge`（独立） | **FAIL**（F1・F7 の未修復＋Important disposition 記録の不在） |

### Critical（7 件）

| ID | 指摘 | disposition |
|---|---|---|
| F1 | 「17 項目」は全文書で偽（実体 16）。**「全項目非空でないと追記しない」規律が充足不能になる** | **修正**。issue #19 本文の箇条書きは実測 16 件（`gh api` で再確認）＝計数誤りであり設計選択ではない。全 18 ファイルを 16 へ訂正。**ラウンド 1 の初回修正で 8 箇所を取りこぼし、採点者が検出→再修正**（見出し `Draft 17 Fields`・`17 フィールド`・`17 に満たない`・DFD `10 → 17`） |
| F2 | 非障害テンプレに `要因分類`（What 軸）が無く、横断集約の 4 軸のうち 1 軸が母集団の半分で欠測 | **修正**。項目 8 の内訳に `要因分類` を追加（障害側は元から両ラベルを持つ——揃えただけで新項目ではない）。実走 entry にも追記 |
| F3 | 起動トリガが「同じ ID・違う中身」で二重定義。正本宣言も相互に逆を向く | **修正**。ID の正を steering §6 に一本化（6 個）。`trigger-detection.md` は「同じ ID の判定ロジックを持つ実装細目」と位置づけ。**注記の ID 割付が 1 箇所残存し採点者が検出→再修正** |
| F4 | (c)(d) は「キューに積むだけ」だが**キューは算出値ゆえ積む先が存在しない**——すり抜けの観測が消える | **修正**。`.kiro/analysis/INDEX.md` に「**すり抜け検出ログ**」節を新設し、検出時にその場で 1 行残す規約を steering §2.1／§6／skill／`trigger-detection.md` に配線。あわせて (d) `post-close` が実体を持たない no-op であることを**正直開示**に降格 |
| F5 | `kiro-postmortem-review` が本文で要求する操作（Write／AskUserQuestion／skill 起動）のツールを持たない | **修正**。`allowed-tools` に `Write`・`AskUserQuestion`・`Skill` を追加 |
| F6 | 障害 entry の INDEX 行を書く工程が存在せず「入口 1 か所」が空文 | **修正**。A4 の障害枝に「委譲後、本フローが `INDEX.md` へ 1 行 append」を明記（steering §1・skill・両 README） |
| F7 | 母集団が二重定義（全クローズ issue vs すり抜けのみ）＋入口フィルタが循環 | **修正**。フィルタの適用範囲を「**欠陥検出を契機とする追加投入のみ**」に限定し、循環は「投入時点の工程判定は暫定でよい」で切断。**ラウンド 1 では steering と DFD しか直さず、D-84・正本 R4.5・両ヘッダに残存→採点者が検出→再修正**（D-84 は表題・決めたこと・判定文・外すとどうなるかを改訂） |

### Important（13 件）

| ID | 指摘 | disposition |
|---|---|---|
| F8 | status 語彙が 2 系統あり、非障害 entry が終端に到達できない | **修正**。非障害は `drafted → ratified → aggregated`、障害は `recorded → reviewed → steered`。「混ぜない」と steering §9 と review skill Step 8-2 に明記 |
| F9 | `Schema:`／`schema:` の大小文字差で v2 の非障害 entry が全部 v1 と誤判定されうる | **修正**。判定を**大小文字非依存**（frontmatter 含む）と review skill に明記 |
| F10 | 「正本は 1 ファイル」と言いながら規範が値集合を再列挙し、対象システムが 5 対 6 で既に食い違い | **修正**。steering §3/§5 の値列挙を**正本へのポインタ**に置換（軸の一覧のみ保持）。ヘッダ 2 種も同様に軸だけへ縮退 |
| F11 | 削除したはずの「同期義務つき複製」が `ledger-header.md` に第 3 の複製として生存 | **修正**。ヘッダ／テンプレのタクソノミー節を**軸だけのポインタ**にし、拡張手順に「正本を更新すれば足りる（軸を増減したときだけ要追随）」と明記 |
| F12 | 来歴 DFD が自らの裁定 B-j を 3 箇所で否定（to-be 図・フォルダ構成・保存先表） | **修正**。3 箇所とも B-j 後の姿（正本 1 ファイル）へ改訂 |
| F13 | 実走 entry の `derived` 欄（項目 9）が事実誤り——「全 15 本」に `#43`（旧リポ）を含み自己矛盾 | **修正**。「クローズ済み 13 本（#43 は旧リポゆえ除外・#19 は進行中）」へ訂正し、出典コマンドを明記。訂正履歴を entry 末尾に記録 |
| F14 | HX「4 群だけ」が委譲先の全項目確認ループと衝突 | **修正**。`kiro-postmortem-add` Step 4 に「`Source: analysis-intake` の場合は HX の 4 群のみ確認・残りは表示のみ」を追加。steering §1 HX にも同旨 |
| F15 | `captured` が §3 の充填系統表に無く、P6 採取値と HX 判定の優先順位が未定義 | **修正**。項目 3/4 の系統欄を「`captured`（採取がある場合）／無ければ `inferred`。いずれも HX で確定」とし、§2 に「**食い違ったら HX が正・採取値と覆した理由を残す**」を追加 |
| F16 | 「一方向の依存」宣言が A5 の委譲辺（→ `kiro-postmortem-review`）を落としている | **修正**。§10 に同委譲辺を明記し、**直接起動時は新規 entry を作らない**（集約と Try 抽出のみ）と定めた |
| F17 | 台帳の非正典宣言（「根拠として引用しない」）と `derived` の出典が正面衝突 | **修正**。`moira/changes/README.md` に限定を明記——「禁じるのは**現況の主張**。**その時点にそう記録されていた**という事実の出典としての引用は可」。steering §4 の `derived` 定義にも同旨 |
| F18 | 「算出値なら陳腐化しない」は re-open で破れる（closed 集合から外れて母集団から消える） | **修正**。§6 に**正直開示**として明記し、「entry の `analyzed-at` より後に再クローズされたキーは未分析として再計上する」規則を追加（`trigger-detection.md` の擬似コードにも反映） |
| F19 | steering の「人間承認」表示が、実際に得た承認（DFD v3）より強い | **修正**。状態行を「**確定前**——設計は承認済みだが**文面は doc-refine ゲート内で確定**・D-82〜86 は `proposed`」へ書き換え |
| F20 | README の正直枠 #2 が因果を誤り（「履歴に無い → `unknown`」）、初回実走（`unknown` 0）に反証されている | **修正**。「履歴に無い 7 項目は根拠を示せる限り `inferred` で埋まる。`unknown` は根拠そのものを示せないときだけ」へ因果を訂正 |

### Minor（4 件）

| ID | 指摘 | disposition |
|---|---|---|
| F21 | `reviews/` が未作成で `periodic` が常時発火。判定ソースが表と擬似コードで割れる | **修正**。「集約が一度も無いときは発火させない」を steering §6・`trigger-detection.md`（表・擬似コード）に統一。`reviews/` 未作成は README で開示（初回集約時に作成） |
| F22 | 「記録 → 集約 → 反映の 4 段構成」が 3 段しか数えていない | **修正**。「Plan → Do → Check → Act の 4 フェーズ（記録 → 集約 → 反映）」へ |
| F23 | DFD のフォルダ構成にテンプレート名の誤り（`index.template.md`）と改訂ファイルの記載漏れ | **修正**（テンプレ名）。**部分的に deferred**: 索引ツリーへの `DECISIONS-CATALOG`／`structure`／`moira-model`／`moira-verification`／`changes/README` の追記は行わず、**保存先の表と影響マップが正**とした（DFD §10.1 はフォルダ構成の図であり全変更ファイルの一覧ではない——健全な反証） |
| F24 | `periodic` の起点が文書間で違う（前回分析 vs 前回集約） | **修正**。「**前回の横断集約**（`reviews/` の最新日付）」に統一 |

### 事実検証の CORRECTED（6 件）

| 主張 | 正値 | disposition |
|---|---|---|
| 「17 項目」 | **16 項目** | **修正**（F1 と同一） |
| 「クローズ済み 15 本」 | **13 本** | **修正**。DFD 4 箇所・影響マップ・実走 entry・**D-83 本文**（採点者が検出→再修正） |
| 「`#39`・`#42` が旧リポ」 | **`#39`・`#42`・`#43` の 3 本すべて** | **修正**（影響マップの訂正記録・実走 entry） |
| 「#16 の `gate-round-records.md`」 | **同ファイルは #16 に存在しない**（実出典は同 issue の `closure-report.md`）。同ファイルを持つのは本リポ **8 本** | **修正**（DFD F1 行の証拠列・影響マップの訂正記録） |
| 「本リポの同番号と**衝突する**」 | **衝突しうる**（本リポ最大 issue 番号は #20 ゆえ実衝突は未発生） | **修正**（steering・DFD・`.kiro/analysis/README.md`——最後の 1 件は採点者が検出→再修正） |
| 「`metadata.origin` 未設定」 | **設定されている**（両ファイル） | **健全な反証**（現物確認済み。指摘が誤り） |

### 追跡付き deferred（1 件）

| 事項 | 追跡 | owner | 再評価条件 |
|---|---|---|---|
| `moira/examples/todo-playground/.claude/skills/kiro-postmortem-add/` に**旧版一式が生きた手順文のまま残存**（本 issue が是正した seed 自動投入・`.kiro/specs` 依存を含む） | **[#20](https://github.com/PrimeBrains/moira/issues/20)**（open） | リポジトリオーナー | サンプル資材の skill を本体と同期する方針か固定する方針かを決めた時点 |

**deferred の根拠**: 当該パスは**別プロジェクト向け配布サンプル資材**であり、本 issue の影響マップの対象外
（スコープを広げると未マップ差分になる）。同期方針そのものが未決のため、本 issue 内で判断しない。

## ラウンド 2（採点者の FAIL 指摘への対応）

| 採点者の指摘 | disposition |
|---|---|
| F1 未決着（`17` が 5 ファイル 8 箇所に残存） | **修正**（上記 F1 欄に追記済み） |
| F7 未決着（母集団二重定義が D-84・正本 R4.5・`defects.md` ヘッダ・`ledger-header.md` に残存） | **修正**。D-84 の表題・決めたこと・外すとどうなるか・**判定する一文**を、批准済み Q5 の射程（＝欠陥検出を入口にするか否かであって母集団の絞り込みではない）に合わせて改訂。正本 R4.5 と両ヘッダの同文も同様に限定 |
| F3 残骸（`trigger-detection.md` の「(e) user-explicit は含めない」が旧割付） | **修正**（`(f)` へ。あわせて `(d)`/`(e)` も提案対象外である旨を明記） |
| fact-check 未適用 2 件（D-83 の「15 本」・`.kiro/analysis/README.md` の「衝突する」） | **修正** |
| Important 13 件の disposition 記録が存在しない | **修正**。**本ファイルがその記録**（ラウンド 1 の表） |
| 意図整合の逸脱（D-84 の判定文が批准済み意図と矛盾） | **修正**。D-84 を批准済み意図に合わせた（HB 上申は不要——逸脱していたのは**ドラフト側**であり、批准記録 Q5 が正。`fork-batch.md` の FORK-2 決着と同じ読み） |
| `fork-batch.md` の決着報告が現物と不一致 | **修正**。同ファイルに「是正の実行状況」を追記し、ラウンド 2 で完了したことを明記 |
| DFD の状態行が v3 のまま（陳腐化。FAIL 基準にはしないと明記された観察） | **修正**（v4・承認後の実装工程であることを明記） |
