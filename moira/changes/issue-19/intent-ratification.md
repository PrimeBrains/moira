---
status: working-ledger
issue: 19
---

# 意図批准記録 — issue #19（HA 前半集約セッション・**準備稿**）

> 本稿は HA 提示用の準備稿。裁定後に「批准済み」として確定し、issue コメントへ要約を残す。

## ① 影響マップ人間断面の確認（「はねる先はこれで全部か」）

**人間が読む面（3面）**は実質 1 行に集約される:

- **設計判断（D）**: 新機構について**新しく決めること**が 5 つある（下記 ④）。
- **モデル・不変条件・シナリオ（M/P/S）**: いずれも**変わらない**ことの確認のみ。本件は開発プロセス側の
  道具であり、Moira 製品のふるまい・数え方・予測ルールに触れない。

**機械決着に回す面**: 確定文書 10 行（DFD 文書・新 skill・台帳 README・関連 steering／CLAUDE.md の索引追随・
既存 postmortem 側の境界注記）は `doc-refine` ゲート内で批准する（HA 対象外）。コードは**書かない見込み**。

詳細: [`impact-map.md`](impact-map.md)（全 16 行＋前提調査＝17 項目の導出可能性の実地監査）

## ② 境界裁定（諮る論点）

**Q1 既存の「不具合の記録簿」との役割分担**（`.kiro/postmortem/defects.md`・10 項目・不具合限定）。
本件の 17 項目はその近い上位集合だが、「障害／**非障害**」の判定欄と「要件追加・リファクタリング…」の
変更分類欄があるため、**母集団が「不具合」から「全変更」へ広がる**——既存簿の定義と両立しない。

**Q4 「変更範囲」の言葉づかい**: issue 本文は S/P/D/C の 4 値だが、本リポの既存分類は M/D/P/S/C/V/F の 7 値
（4 値はその部分集合）。競合する分類語彙を 2 つ持つか、既存に統一するか。

## ③ When/Then の発案

**該当なし**——新規シナリオを起こさない（プロセス機構であり、画面・CLI の観測ふるまいが変わらない）。
この判断自体が S 行（R14）の批准対象。

## ④ 意図批准（D 級・「何を決めるか」と受け入れ基準）

| # | 決めること | 受け入れ基準（falsifiable） | 却下したい方向 |
|---|---|---|---|
| D-a | 分析結果の置き場と所有権、既存 postmortem との依存方向 | 両側の文書に境界が明記され、依存が**一方向**であること（相互依存を作らない） | 2 つの要因分析機構が黙って併存し、同じ欠陥が両方に別々に記録される状態 |
| D-b | 履歴に存在しない 7 項目（障害判定・変更分類・根本要因・再発防止策・検知系 3 項目）の埋め方 | 各欄に**出所ラベル**（履歴から/AI 推論/実行時採取/不明）が付き、**根拠を示せない欄は `unknown` と記録される**こと。空欄の穴埋め・推測の断定を許さない | 「履歴を読めば全部わかる」かのような台帳（後知恵の推論が一次記録に偽装される） |
| D-c | 起動トリガ（「定期的に」の実装） | AI が**ユーザー確認なしに分析を起動しない**こと。かつクローズ工程を重くしないこと | クローズのたびに重い分析が走る／逆に誰も起動せず死蔵する |
| D-d | 分類語彙 | 既存語彙と**競合する第 2 の分類体系を作らない**こと（タクソノミーの定義場所が 1 か所） | 同じ概念に 2 つのラベル体系が並立する |
| D-e | issue キーの名前空間 | キーが repo 修飾（`moira#16`）であり、旧リポ（`sdd-workshop#39`）と**衝突しない**こと（`moira/changes/issue-39/42/43` は旧リポ番号——実地確認済み） | 無修飾 `#N` により同件調査とリンクが黙って誤対応する |

## ⑤ 実行計画（経路列・依存順）と一次資料セット

**経路列（依存順）**:

1. **HA 裁定**（Q1〜Q4）→ DFD ドラフト（[`moira/plans/2026-07-25-change-analysis-dfd.md`](../../plans/2026-07-25-change-analysis-dfd.md)）を裁定どおりに確定 → **人間承認**（issue 本文の順序拘束）
2. **D 級**: `moira/DECISIONS-CATALOG.md` に D-a〜D-e を `proposed` 起票 → `doc-refine` → `agreed`
3. **F 級**: DFD 文書・新 skill・台帳 README／様式・関連 steering／CLAUDE.md の索引追随・postmortem 側の境界注記 → `doc-refine`（1 ゲートで束ねる）
4. **M/P/S/C 級**: 変更なし照合（MODEL・PROPERTIES・シナリオ・コード）
5. **ドッグフーディング**: 新機構で**実走 1 本**（直近クローズ issue 1 件）——**本フローのゲートには使わない**（自己検証禁止と同趣旨）
6. **P5 同期閉包 → H5 クローズ**

**一次資料セット（SOURCE_SET_CONFIRMED の根拠。`doc-refine` 系ゲートが依拠してよい資料）**:

- `.kiro/steering/moira-change-management.md`（変更管理フローの規範）
- `.kiro/steering/moira-verification.md`（計器構成・ゲート対応表）
- `.kiro/postmortem/defects.md`＋`.claude/skills/kiro-postmortem-{add,review}/`（既存の要因分析機構）
- `.claude/skills/moira-change/`（振り付けの先例）＋`moira/plans/2026-07-19-change-management-dfd.md`（DFD の先例）
- `moira/changes/issue-*/`（実地監査の対象＝一次観測）
- `moira/changes/issue-19/impact-map.md` §前提調査（本件の一次観測の記録）

## 裁定結果（HA 第 1 ラウンド・2026-07-25）

| # | 論点 | 裁定 | 備考 |
|---|---|---|---|
| Q1 | 置き場と既存「不具合の記録簿」との分担 | **障害と非障害で別管理**。ただし**前段に「障害か非障害か」を振り分ける受付を新設**する。**加えて既存の障害フロー自体も DFD に起こし、正しいかを改めて検討する**（ユーザー原文: 「障害と非障害で別管理するってことね。であれば、障害か非障害かを最初に振り分ける受付が必要だね。あと既存の障害フローも正しいか自信ないから dfd におこして改めてちゃんと考えたい」） | **スコープ拡大**——影響マップに R17〜R20 を追記。DFD は v2 で §2（A0 受付）・§3（既存フローの as-is＋欠陥 8 件＋改訂案）を新設 |
| Q2 | 履歴に無い 7 項目の埋め方 | **AI 推論＋人間確認（HX で 4 群のみ）＋一次採取は 2 欄だけ追加**（障害判定・変更分類を P6 クローズ時に採取） | 推奨どおり。DFD §5・§8 |
| Q3 | 起動トリガ | **未分析が 10 件たまったら提案**（ユーザー裁定値。推奨の 3 件から変更） | DFD §7 (a) |
| Q4 | 変更範囲の語彙 | **既存 7 値 M/D/P/S/C/V/F に統一** | DFD §5 #5 |

**D-a〜D-e の受け入れ基準に対する裁定の含意**:

- D-a（分担）: 「別管理＋共通受付＋共通項目定義・共通タクソノミー」で受け入れ基準（境界の明記・**一方向**依存）を満たす。
  依存方向は **新 skill → 既存 `/kiro-postmortem-add`（障害 entry の記録を委譲）** の一方向。
- D-b（欠落項目）: 出所ラベル 4 値（`derived`/`inferred`/`captured`/`unknown`）で受け入れ基準（根拠なき欄は
  `unknown`・空欄の穴埋め禁止）を満たす。
- D-c（トリガ）: 閾値 10 件・P6 クローズは「積むだけ」・AI はユーザー確認なしに起動しない、で受け入れ基準を満たす。
- D-d（語彙）: 既存 7 値に統一＋タクソノミー定義場所を 1 か所（既存 postmortem 側）に集約、で受け入れ基準を満たす。
  **あわせて死んだ 3 ファイル目の同期義務を除去**（DFD §3.3 B-e）。
- D-e（キー）: `<repo>#<number>` 修飾で受け入れ基準を満たす。

## 裁定結果（HA 第 2 ラウンド・2026-07-25）

| # | 論点 | 裁定 |
|---|---|---|
| Q5 | 欠陥検出を入口にする範囲（ユーザー指摘: 「ゲート/CI/検知器の検出も入口にすると、実装中の試行錯誤まで対象になって膨大にならないか。変更管理のプロセスだけを対象にするでもいい気がするが、それだとゆるいか」） | **すり抜けギャップのあるものだけを入口にする**——判定式は「**検知すべき工程 ≠ 実際に検知した工程**」。ラウンド内で決着した指摘・実装中のテスト失敗・P5 差し戻しは**入口にしない**（工程が設計どおり働いた証拠）。クローズ済み変更のすり抜け発覚・deferred/intent-drift の実害化・マージ後の CI 赤は**入口**。DFD §2.1・§7 (d) |

**この裁定が満たす受け入れ基準**: ①母集団が試行錯誤で汚れない ②「変更管理プロセス内の事象だけ」に絞った場合に
落ちる**最も価値の高い種類**（過去の変更のすり抜け・撤回条件の実害化）を取りこぼさない ③新しい判定基準を
発明せず既存スキーマの項目 13/14 をそのまま判定器に使う ④AI は都度確認せずキューに積むだけ。

## 裁定結果（HA 第 3・第 4 ラウンド・2026-07-25）

| # | 論点 | 裁定 |
|---|---|---|
| Q6 | 既存障害フローの **to-be DFD** が無い（ユーザー指摘） | **to-be DFD を §3.4 に追加**（除去・廃止されるものを赤破線で、不変のものを「（不変）」で明示）。3 枚とも mermaid でレンダリング検証済み（構文エラー 0） |
| Q7 | **フォルダ構成**（どこに何を保存するか）の明示（ユーザー要求） | **DFD §10.1 を追加**。規範＝`.kiro/steering/moira-change-analysis.md`／障害台帳＝`.kiro/postmortem/defects.md`（場所据え置き）／非障害台帳＝`.kiro/analysis/`（README・INDEX・entries/・reviews/）／振り付け＝`.claude/skills/moira-change-analysis/`。**未分析キューは保存しない（算出値）**。台帳を `.kiro/` に置くのは「`moira/`＝製品／`.kiro/`＝プロセス」の既存分担に従うため |
| **承認** | **DFD v3（全体・as-is・to-be の 3 枚）＋フォルダ構成＋実行計画** | **承認（2026-07-25）**——実装に着手してよい。issue 本文の順序拘束「まず DFD をつくってユーザーの承認を得てから実装に移る」を満たした |

**新たに生じた D 論点（第 1 ラウンドの裁定から派生）**:

- **D-f 既存障害フローの改訂範囲**: 「配線の是正に絞り、PDCA 思想（4 フェーズ・Status 3 値・append-only・
  全項目非空での append 拒否・`/kiro-steering-custom` 経由でのみ steering を触る規律）は変えない」を
  受け入れ基準とする（DFD §3.3 の「改訂しないもの」節）。
- **D-g 既存 entry の遡及**: 既存 entry #0001 は **as-is 保持**（10 項目のまま・byte 不変）。
  遡及書き換えをしないことを受け入れ基準とする。
