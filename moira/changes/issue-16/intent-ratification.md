---
status: working-ledger
issue: 16
---

# 意図批准記録 — issue #16（HA 前半集約セッション）

（2026-07-24 HA 実施済み——全項目批准。裁定は
[GitHub コメント](https://github.com/PrimeBrains/moira/issues/16#issuecomment-5064510592) に記録済み。
本ファイルはそのコメントからの再構成——request.md の再構成開示を参照。コメントの R 番号〔R5=MODEL/
R6=D-79/R7=プロパティ〕は本台帳の通し番号〔R7=MODEL/R8=D-79/R9=プロパティ〕へ読み替える。実質同一。）

## ① 影響マップ確認

- 確認: yes（2026-07-24）——「はねる先はこれで全部」を確認（全 10 行——コード 6・文書 2〔MODEL §7#20 追補・
  D-79 注記〕・確認のみ 2〔プロパティ集・シナリオとも変更なし〕）
- 指摘・追加された波及先: なし
- 補足（正直開示）: `.moira` の他ファイル群（capacity/dates/milestones/labels/members/config）は events.json と
  同型の torn write（一部は lost update も）を持つが、issue #16 のスコープ外。②境界裁定 (c) のとおり
  **issue #17 として起票して追跡**する（下記⑤欄参照）。

## ② 境界裁定

| 行 ID | 争点（平易文） | 裁定 | 日付 |
|---|---|---|---|
| R2/R3 | events.json の書き込み保護は、既存判断 D-11（「永続化方式を決める責任は土台が持つ」）の範囲内の実装詳細として扱い、新しい設計判断は起票しない。残作業 2（並行書き込み保護）の結論として **advisory lock を events.json にも適用する**（#15 機構の同水準適用）——でよいか | 承認——D-11 範囲内の実装詳細・新規 D 起票なし。残作業 2＝advisory lock を events.json へ適用（#15 の自前 fs ロック機構をパス汎用化して同水準適用）。torn write 側はアトミック置換（temp→rename）で塞ぐ | 2026-07-24 |
| R10 | events.json 書き込みのアトミック化・排他はユーザー可視のふるまいを変えない。#15 R9 の判断（永続化耐久性・訂正機能のシナリオ整備は対象外/将来一括）を踏襲し、新しいシナリオ/E2E は起こさない——でよいか | 踏襲する——S 級新規は起こさない（シナリオ/E2E 波及ゼロ承認） | 2026-07-24 |
| — | スコープ外残余（.moira 他ファイルの torn write/lost update 露出）の扱い | **issue #17 起票で追跡**（本フローのスコープ外・#15→#16 と同型の正直開示の連鎖） | 2026-07-24 |

## ③ S 級 When/Then の発案

該当なし（R10 裁定＝新規シナリオなし・既存 unit の変更もなし）。

## ④ 意図批准（M/D/P 対象行）

| 行 ID | 対象クラス | 何を決めるか | 受け入れ基準 | 却下したい方向 | 批准 | 日付 |
|---|---|---|---|---|---|---|
| R7 | M | モデル文書 §7#20 の実装状態メモ「イベントログ本体の同型書き込み保護はスコープ外——issue #16 で追跡」を、実装完了後に「実装済み」へ書き換える | (1) ルール本文（§2.10 の訂正原理・公理・不変条件・訂正計器規則）は一字も変えない (2) 日本語版と英語版の 2 箇所が同期する (3) 既存の追補と同じ形式（日付つき追補） (4) スコープ外残余（.moira 他ファイル）を issue #17 として正直に開示 | ルール本文の変更・新ルールの追加・追跡記述の黙った削除（追補で上書きせず消すこと） | yes | 2026-07-24 |
| R8 | D | 設計判断カタログ D-79 の「イベントログ本体の同型保護はスコープ外・issue #16 で追跡」メモを実装完了の記述へ更新する | (1) 判断の一文・判定文・正直な開示欄は不変 (2) 新旧 #16 の読み分け注意（D-80）は保持 (3) 何が実装されたか（events.json のアトミック置換＋advisory lock）を一言で記す (4) .moira 他ファイル残余の #17 追跡を添える | 判断一文の書き換え・#16 参照の黙った削除 | yes | 2026-07-24 |
| R9 | P | プロパティは新設せず文面も変えない——書き込みの耐久性はプロパティ集の対象外という「変更不要」を確定する | (1) プロパティ集に差分ゼロ (2) #16 言及ゼロ | 今回のついでの新プロパティ起案 | yes | 2026-07-24 |

## ⑤ 実行計画承認＋一次資料セット確定

- 実行計画（経路列・依存順）:
  1. **C 面**（R1→R2→R3・R4・R5・R6 確認）→ `/kiro-impl`（本環境では codex 非在のため独立敵対レビュー
     subagent で代替）＋CI。実装方式（受け入れ基準）: (i) アトミック置換 primitive を土台
     （`moira/backend/src/atomic-write.ts`）に新設し EventStore.saveJson が使う（D-11） (ii) #15 の corrections
     専用 advisory lock を **パス汎用化**（`acquireLock(dataPath)`/`releaseLock(dataPath, token)`/`lockPathFor`）
     し appendEvents に適用 (iii) appendCorrections は共有ヘルパへ委譲（DRY・挙動不変）
  2. **M 面**（R7）→ `moira-model-update`（実装完了後の実装状態追補・日英同期・#17 残余開示）
  3. **D 面**（R8）→ `doc-refine`（DECISIONS-CATALOG D-79 注記同期）
  4. **P 確認**（R9）→ 照合 worker で「差分ゼロ」を確認（P5 内）
  5. P5 同期閉包 → P6 クローズ
  - **依存順の注記**: 既定の依存方向（M→D/P/S→C）と逆に見えるが、本 issue の M/D 行は
    **規範変更ではなく実装状態の追補**であり、実装が完了して初めて真になる記述——実依存は C→注記。
    §2.10 の規範自体は不変で、C が依拠する正典は動かない（#11・#15 のゲート順と同型）。
- 一次資料セット: moira/MODEL.md（v22・§2.10・§7#20）／moira/DECISIONS-CATALOG.md（D-11・D-79・D-80）／
  moira/PROPERTIES.md（v0.7）／issue #16 本文。背景（非正典・根拠にしない）: moira/changes/issue-11/・
  issue-15/ 台帳（corrections 側の前例）
- 承認: yes（2026-07-24）
- スコープ外開示の起票: .moira 他ファイルの同型レースは https://github.com/PrimeBrains/moira/issues/17 として
  起票済み（②境界裁定 (c) どおり）

## issue コメントへの要約

- issue コメント: https://github.com/PrimeBrains/moira/issues/16#issuecomment-5064510592
