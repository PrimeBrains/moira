---
status: working-ledger
issue: 15
---

# 意図批准記録 — issue #15（HA 前半集約セッション）

（準備稿——HA 裁定後に「批准」欄と日付を確定する）

## ① 影響マップ確認

- 確認: （HA 待ち）
- 指摘・追加された波及先: （HA 待ち）
- 補足（正直開示）: events.json の書き込みは corrections.json より脆弱な同型レース
  （素の writeFileSync・rename なし）を持つが、issue #15 のスコープ外。新 issue 起票の要否を
  HA で確認する。

## ② 境界裁定

| 行 ID | 争点（平易文） | 裁定 | 日付 |
|---|---|---|---|
| R9 | 今回の変更でユーザーから見える表示（適用不能カウント・重大表示）は変わるが、これを描く受け入れシナリオは現状存在しない。前回 issue #11 の判断「訂正機能のシナリオ/E2E 整備は将来まとめて行う」を今回も踏襲し、新しいシナリオは起こさない——でよいか（起こす場合は kiro-scenario 行を追記して When/Then 発案から行う） | （HA 待ち） | — |
| R3 | 訂正ファイルの同時書き込み保護は、既存判断 D-11（「保存のやり方は土台が所有する」）の範囲内の実装詳細として扱い、新しい設計判断は起票しない。実装は新規ライブラリを増やさない自前のロックファイル方式（既存コードの流儀に合わせる）——でよいか | （HA 待ち） | — |

## ③ S 級 When/Then の発案

R9 の裁定が「新規シナリオなし」なら該当なし。「起こす」裁定の場合のみ、ここで When/Then を発案
いただく（新規シナリオは人間発案が必須）。

## ④ 意図批准（M/D/P 対象行）

| 行 ID | 対象クラス | 何を決めるか | 受け入れ基準 | 却下したい方向 | 批准 | 日付 |
|---|---|---|---|---|---|---|
| R5 | M | モデル文書の実装状態メモ「残り 3 例の入口検証・同時書き込み保護は issue #15 で追跡中」を、実装完了後に「実装済み」へ書き換える | (1) ルール本文（「不正な訂正は適用不能で、先行の有効な訂正がそのまま残る」等の規範・公理・不変条件）は一字も変えない (2) 日本語版と英語版の 2 箇所が同期する (3) 既存の追補と同じ形式（日付つき追補） | ルール本文の変更・新ルールの追加・追跡記述の黙った削除（追補で上書きせず消すこと） | （HA 待ち） | — |
| R6 | D | 設計判断カタログ D-79 の「残余は #15 で追跡」メモを実装完了の記述へ更新する | (1) 判断の一文・判定文は不変 (2) 新旧 #15 の読み分け注意（D-80）は保持 (3) 何が実装されたか（3 例の入口検証・同時書き込み保護）を一言で記す | 判断一文の書き換え・#15 参照の黙った削除 | （HA 待ち） | — |
| R7 | D | 設計判断カタログ D-1 の「#15 を参照」メモを解消後の状態へ整合させる最小の追補 | (1) 判断の一文は不変 (2) D-79 側（R6）との整合 | 判断一文の書き換え・過剰な本文改稿 | （HA 待ち） | — |
| R8 | P | プロパティは新設せず文面も変えない——既存の約束のテスト証拠（witness）が増えるだけ、という「変更不要」を確定する | (1) プロパティ集に差分ゼロ (2) 既存 bound プロパティ（訂正 4 区分計数・再生同一性・循環拒否）の witness が backend テストで増える（R2 が担う） | 今回のついでの新プロパティ起案 | （HA 待ち） | — |

## ⑤ 実行計画承認＋一次資料セット確定

- 実行計画（経路列・依存順）:
  1. **C 面**（R1→R2・R3→R4・R10 確認）→ `/kiro-impl`（worker=sonnet）＋codex レビュー＋CI。
     実装方式（受け入れ基準）: pre-admission は**試行 fold**（base switch を再利用し、候補訂正を
     仮適用した effective stream を fold して構造エラー集合の**差分**〔件数でなく対象・種別の識別〕で
     帰属判定。record 単位で全部 or 無）。lock は fs ベース advisory lock（`wx` 排他生成→
     critical section→finally unlink・EEXIST リトライ・stale 検知。新規ランタイム依存なし）
  2. **M 面**（R5）→ `moira-model-update`（実装完了後の実装状態追補・日英同期）
  3. **D 面**（R6・R7）→ `doc-refine`（DECISIONS-CATALOG 注記同期）
  4. **P 確認**（R8）→ 照合 worker で「差分ゼロ」を確認（P5 内）
  5. P5 同期閉包 → P6 クローズ
  - **依存順の注記**: 既定の依存方向（M→D/P/S→C）と逆に見えるが、本 issue の M/D 行は
    **規範変更ではなく実装状態の追補**であり、実装が完了して初めて真になる記述——実依存は C→注記。
    §2.10 の規範自体は不変で、C が依拠する正典は動かない（#11 のゲート順と同型）。
- 一次資料セット: moira/MODEL.md（v22・§2.10・§7#20）／moira/DECISIONS-CATALOG.md
  （D-1・D-11・D-37・D-63・D-79・D-80）／moira/PROPERTIES.md（v0.7）／issue #15 本文。
  背景（非正典・根拠にしない）: moira/changes/issue-11/ 台帳
- 承認: （HA 待ち）

## issue コメントへの要約

- issue コメント: （HA 後に記録）
